{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Users List</h1>

    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for utilisateur in utilisateurs %}
            <tr>
                <td>{{ utilisateur.username }}</td>
                <td>
                    {% if utilisateur.adresses|length != 0 %}
                        <a href="{{ path('adminAdressesUtilisateurs', { 'id': utilisateur.id }) }}" title="View Addresses"><i class="icon-book"></i></a>
                    {% endif %}
                    {% if utilisateur.commandes|length != 0 %}
                        <a href="{{ path('adminFacturesUtilisateurs', { 'id': utilisateur.id }) }}" title="view invoices"><i class="icon-book"></i></a>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}